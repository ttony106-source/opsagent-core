{
  "policyset_id": "opsagent-baseline",
  "version": "v1.0.0",
  "status": "active",
  "policies": [
    {
      "policy_id": "POL-EXEC-001",
      "action": "approve_release",
      "mode": "FAIL_CLOSED",
      "requires": {
        "authority_role": "CEO",
        "payload_fields": ["change_ticket"]
      },
      "checks": ["authority_role", "required_fields"],
      "evidence_required": ["run_entry.json", "payload.json", "decision.json"],
      "on_deny": "DENY"
    },
    {
      "policy_id": "POL-EXEC-002",
      "action": "audit_snapshot",
      "mode": "ANY",
      "requires": {
        "payload_fields": ["scope"]
      },
      "checks": ["required_fields"],
      "evidence_required": ["run_entry.json", "payload.json", "decision.json"],
      "on_deny": "DENY"
    }
  ]
}
